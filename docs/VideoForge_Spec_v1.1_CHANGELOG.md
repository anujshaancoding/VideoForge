# VideoForge Spec v1.1 — Changelog (corrections from v1.0)

> Generated from a multi-agent audit of the original ClipForge v1.0 spec: **111 raw findings → 92 confirmed** (19 rejected as false positives after adversarial verification). Renamed product **ClipForge → VideoForge**.

**By severity:** 32 high · 39 medium · 21 low  
**By type:** 22 errors · 36 inconsistencies · 23 gaps · 11 ambiguities

Plus **6 new sections** added to fill gaps: 18 Data Model & Schema, 19 Accessibility, 20 Observability/Analytics, 21 Billing & Monetization, 22 Testing & QA, 23 Non-Goals & Roadmap (glossary moved to 24).


## High-severity (32)

| ID | Section | Type | Issue | Resolution (summary) |
|----|---------|------|-------|----------------------|
| A-3 | 1.1 vs 2.2 vs 15.2 | inconsistency | Max output resolution conflicts: 4K (1.1), 4096x4096 custom cap (2.2), and 8K Enterprise (15.2) | Pick one ceiling and propagate it consistently. Two clean options:  OPTION A — 8K is real (keep Enterprise 8K in 15.2): 1. Section 1.1 (line 32): change "supporting up to 4K/60fps output" to "supporting up to 8K/60fps output (8K on Enter… |
| A-1 | 1.3 / 3.2 / 7.1 | error | PannerNode used for stereo pan, but PannerNode has no .pan property | Two edits to align all three sections on StereoPannerNode (Section 7.1 line 671 is already correct and needs no change):  1. Section 1.3 (line 58): change "per-track GainNode, PannerNode, and AnalyserNode" to "per-track GainNode, StereoP… |
| D-5 | 10.1 | gap | 5.1 Surround export references a 'surround track' concept that exists nowhere in the spec | In Section 10.1, Format & Quality tab, change the "Audio Channels" row from "Stereo (default) / Mono / 5.1 Surround (if project has surround tracks)." to "Stereo (default) / Mono." This removes the unbuildable 5.1 option and its undefine… |
| D-8 | 10.2 | gap | Export pipeline never re-encodes from originals despite 'Proxy to source' default ON, and originals may be unavailable | Do NOT adopt the finding as written; its PROXY_FAILED and voiceover examples are misreads (PROXY_FAILED retains the original per line 1465; recorded webm/opus IS the original per lines 294/418/910). Tightened minimal fix limited to the g… |
| D-4 | 10.2 / 15.3 / 17.2 | inconsistency | Export download URL expiry stated as both 7 days and 1 hour | Adopt one model and reference it from all four sites: user-facing download availability = 7 days, but the signed S3 URL itself is short-lived (1 hour) and minted on demand each time the user requests a download, with the client refreshin… |
| E-7 | 11.1 / 3.2 / 7.1 | gap | Track data model has no field for per-track volume or pan | In Section 11.1, extend the Track object schema for audio-bearing tracks (audio tracks and the dedicated Voice-Over track) to add three persisted fields, matching the ranges/defaults already stated in 3.2 and 7.1: - `volume`: number, 0–2… |
| E-1 | 11.2 | error | "Last-write-wins ... using Operational Transform" conflates two mutually exclusive strategies | In Section 11.2, replace the conflict-resolution bullet (line 1058) with field-level LWW, which is consistent with the existing Immer-patch broadcast architecture (12.1) and the smallest possible change. New text: "Conflict resolution: i… |
| E-9 | 11.3 / 12.1 | gap | Collaborative "undo only my own operations" is unspecified against intervening remote patches | In Section 11.3, replace the bare "Undo applies inversePatch" for the collaborative case with a defined rebase rule. Before applying a stored inversePatch in a multi-user session, transform it against every remote patch applied since the… |
| E-3 | 12.1 / 11.2 / 16.4 | inconsistency | Soft-lock that "cannot be edited" by others contradicts the simultaneous-edit conflict-resolution path | In Section 12.1, replace the contradictory clause so it matches the already-established optimistic / non-exclusive / LWW design. Change "Other users see the lock and cannot edit it." to something like: "Other users see the lock as a visu… |
| E-12 | 13.2 / 3.2 / 3.3 | inconsistency | Keyboard conflict: S means Split, Solo, and (held) Slip | Do not reassign Solo; it is already a track-header button (the S caption alongside M mute and L lock per lines 180, 233, 239), not a keyboard shortcut, so there is no Split/Solo key conflict. Instead make two minimal clarifications. (1) … |
| E-13 | 13.3 / 13.4 | inconsistency | Keyboard conflict: Ctrl+Shift+G is both "Toggle grid/snapping" and "Ungroup" | Resolve the duplicate Ctrl+Shift+G chord by reassigning the grid/snapping toggle; leave Ungroup as-is so it pairs with Ctrl+G (Group).  1. Section 13.3 (View & Navigation), line 1210: change the binding for "Toggle grid / snapping" from … |
| F-1 | 14.2 | inconsistency | Asset upload API contradicts the upload pipeline in Section 4.2 | Make the two sections describe one contract. Recommended canonical model = the chunked S3 multipart flow from 4.2 (it is the only one that supports the parallel/resumable behavior 4.2 and line 1459 require). Concretely, update Section 14… |
| F-2 | 14.5 | error | WebSocket caption event streams blocks, contradicting word-level-timestamp streaming claimed elsewhere | Make word-level timing a first-class, persisted concept so both progressive streaming and karaoke have a defined transport and storage. Precise, minimal edits: (1) Section 11.1 line 1047 — extend the caption block model to include option… |
| F-5 | 15.1 | error | Safari WebCodecs support claim is wrong and contradicts the architecture's Safari fallback | In Section 15.1, change the Safari row's detail text from "WebCodecs supported from 16.4. SharedArrayBuffer requires COOP/COEP headers (served by platform)." to: "Full support except WebCodecs video decode (uses ffmpeg.wasm fallback — sl… |
| F-6 | 15.2 | inconsistency | Enterprise 8K export is unachievable given the 4096x4096 canvas cap | Minimal, internally-consistent fix: in Section 15.2 plan table (line 1380), change Enterprise "Export resolution" from "8K" to "4K". This aligns the single outlier cell with the rest of the spec, which is uniformly 4K in three other plac… |
| F-10 | 17.2 | inconsistency | Export download URL expiry: 7 days vs 1 hour contradiction | Reconcile to a single model: the download is authorised for 7 days, but each individual presigned S3 URL is short-lived (1 hour) and re-minted on demand by an authenticated download endpoint. This matches the refresh logic already descri… |
| B-1 | 3.2 | error | PannerNode has no .pan property (wrong Web Audio node) | Standardize on StereoPannerNode (the node that actually has a `.pan` AudioParam) in all three places so they agree with the already-correct Section 7.1:  1. Section 3.2, line 237 — replace:    "Mapped to Web Audio PannerNode.pan.value du… |
| B-8 | 3.2 | inconsistency | Mute/Solo not respected on export contradicts the mixing model | Edit Section 3.2, line 240 (Mute/Solo row, "Technical Spec / Notes" cell) to make mute/solo part of the exported mix, consistent with the track-level mix model in 5.3/7.1/10.3.  Replace line 240 with: "Mute/solo state persists in project… |
| B-5 | 3.3 | ambiguity | Key 'S' overloaded for both Split and Slip with no disambiguation | Add an explicit S-key arbitration rule to section 3.3 (and cross-reference it in 13.2). Specify: "When a clip is under the cursor, pressing S enters a pending state. (a) If S is released without the pointer moving more than 4px while hel… |
| D-1 | 4.2 | inconsistency | check-duplicate endpoint does not exist in the API reference | Make Section 4.2 and Section 14.2 name the same endpoint and payload. Preferred minimal fix (reuses the existing call, avoids an extra round-trip): add an md5Hash field to the presign request and an existingAssetId field to its response.… |
| A-5 | 5.1 | error | Claim that playbackRate + detune pitch-corrects fast/slow playback is technically wrong | In Section 5.1, line 467, replace "audio pitch-corrected via Web Audio playbackRate + detune" with a technically accurate description. Minimal precise options:  Option A (pitch-preserving preview, consistent with 7.2): "At rates > 1×: fr… |
| A-4 | 5.2 vs 4.2 | inconsistency | Performance table references a '4K proxy' but proxies are defined as 720p only | Make 5.2 and 4.2 consistent. Minimal, no new architecture: relabel the 5.2 performance rows so the named resolution is the SOURCE/project resolution, while preview always decodes the 720p proxy. Change the three rows to: "1080p source pr… |
| B-3 | 6.1 | error | Contrast/Saturation FFmpeg mapping produces gray image at UI default of 0 | In Section 6.1, replace the defective contrast and saturation mappings with explicit offset formulas that match the existing 0-centered UI default and FFmpeg's 1.0-centered parameters:  - Contrast (line 528): change `eq=contrast=value ma… |
| B-4 | 6.6 | gap | No export/FFmpeg path for rectangular, elliptical, or freeform masks | In Section 6.6, add one "Mask rendering" row (mirroring the Chroma Key row's preview/export format) covering all three mask shapes: state that preview is rendered via a WebGL alpha/stencil shader on the canvas, and that export generates … |
| C-2 | 7.1 / 1.3 | inconsistency | Per-clip vs per-track audio signal chain contradiction | Rewrite the 7.1 prose sentence to define one explicit, unambiguous graph and assign each node to clip vs track level (no other section needs changing — 1.3, 3.2, and 7.1's table are already consistent with this once the prose is fixed). … |
| C-1 | 7.1 / 3.2 / 1.3 | error | PannerNode has no .pan property; Track Pan node type is contradictory across the spec | Standardize on StereoPannerNode for Track Pan everywhere (this is what the authoritative audio-chain Section 7.1 already specifies and what the -100..+100 L/R balance slider requires; PannerNode is a 3D spatializer with no `pan` property… |
| C-16 | 8.3 / 10.3 | gap | No export rendering path defined for Lottie animations and SVG vector overlays | Add a server-side overlay rasterization stage to Section 10.3, executed before the FFmpeg encode (it can run as a step in the existing render-worker job, consistent with the BullMQ/FFmpeg worker model in 10.2). Specifically: for each Lot… |
| X-5 | Section 1.1 vs Section 15.2 vs Section 10.1 | inconsistency | Max export resolution conflict: 4K vision vs 8K Enterprise plan vs no 8K in export dropdown | Correct the single outlier cell. In the Section 15.2 plan table, change the Enterprise "Export resolution" value (line 1380) from "8K" to "4K". This aligns the table with the four converging 4K signals (vision line 32, dropdown line 901,… |
| X-1 | Section 13.3 vs Section 13.4 | error | Keyboard collision: Ctrl+Shift+G bound to two different actions | Keep Ctrl+Shift+G = Ungroup (line 1230-1231) to preserve the standard Ctrl+G/Ctrl+Shift+G group/ungroup pairing. Reassign the grid/snapping toggle on line 1209-1210 from "Ctrl + Shift + G" to "Ctrl + ;" — verified unused anywhere in the … |
| X-3 | Section 15.3 vs Sections 10.2 / 10.4 / 15.2 / 17.2 | inconsistency | Export download URL expiry stated as 1 hour vs 7 days | In Section 15.3 (line 1443), change the export download presigned URL expiry from "1 hour" to "7 days" so it matches Sections 10.2, 10.4, and 17.2. Edit the sentence "S3 presigned URLs for download expire in 1 hour." to "S3 presigned URL… |
| X-4 | Section 18 (Glossary) vs Section 10.1 Advanced Tab | inconsistency | Proxy 'never exported' contradicts the 'Proxy to source' export toggle | Pick one of two directions and apply it consistently; both are minimal.  Preferred (keep the feature, fix the wording) — edit the glossary line 1548 from:   "A lower-resolution, optimised version of source media used for browser preview.… |
| X-2 | Section 5.1 vs Section 3.4 & Section 13.1 | inconsistency | J/K/L max playback speed differs: 4x (playback engine) vs 8x (timeline/shortcuts) | Standardize on the 8×/reverse ceiling that Sections 3.4 and 13.1 already agree on (and which matches the "industry standard" J/K/L note), and fix only the outlier line. Edit /tmp/clipforge_spec.txt line 466 from: "Normal (1×), 0.25×, 0.5… |

## Medium-severity (39)

| ID | Section | Type | Issue | Resolution (summary) |
|----|---------|------|-------|----------------------|
| A-2 | 1.3 vs 15.1 | inconsistency | Safari is described as needing ffmpeg.wasm fallback, but 15.1 says Safari supports WebCodecs | Edit Section 1.3, "Media Decode" row (line 56) to match Section 15.1. Change "WebCodecs API (Chrome/Edge) with ffmpeg.wasm fallback for Safari — decode runs in SharedArrayBuffer worker" to "WebCodecs API (Chrome/Edge, Safari 16.4+) with … |
| D-11 | 10.1 | ambiguity | GIF/audio-only output formats leave dependent settings undefined | In Section 10.1, add a short "Per-format setting applicability" subsection (a matrix keyed by the exact controls already listed) defining, for each Output Format, whether each setting is Shown, Hidden/disabled, or Forced:  - MP3 / WAV (a… |
| D-6 | 10.3 | error | amix is the wrong filter for per-track volume/pan and self-attenuates the mix | In Section 10.3, change the audio bullet to specify the mix is non-normalizing so per-track levels are preserved. Minimal edit: replace "Audio tracks mixed via amix filter with individual volume, pan, EQ, and compressor filters per track… |
| D-12 | 10.3 | error | Canvas 2D blend modes do not map cleanly to FFmpeg, and 'Add/Subtract' blends are claimed in both | In Section 3.2, correct the Blend Mode note (line 215) and add a small mapping table. The preview mechanism should not be stated as globalCompositeOperation for all modes, because Add has no native Canvas 2D blend mode (only the approxim… |
| E-6 | 11.1 / 6.4 | inconsistency | Transition storage location contradicts Section 6.4's "separate object" design | In Section 11.1, remove `transitions[]` from the Clip Objects field list (so it reads: "Per clip: id, sourceAssetId, trackId, startOnTimeline (ms), endOnTimeline (ms), trimIn (ms), trimOut (ms), speed, effects[], keyframes{}, linkedClipI… |
| E-10 | 11.2 / 16.4 | gap | Offline-reconnect merge has no stated base revision for conflict resolution | Reuse the spec's existing server-authoritative document and Immer-patch model; add the minimal concurrency primitive needed for the offline path only.  1) Project Data Model (11.1): add a server-owned monotonically increasing integer fie… |
| E-5 | 11.4 / 15.2 | inconsistency | Auto-version retention stated as flat "30 days" contradicts per-tier version-history limits | In Section 11.4, change the Auto-versions retention note from "Retained for 30 days." to "Retained per plan tier (see Section 15.2: Free 7 days / Pro 30 days / Business 90 days / Enterprise 1 year)." Make Section 15.2 the single source o… |
| E-8 | 11.5 / 17.2 / 12.3 | inconsistency | Project role set differs between sections: missing/overlapping "Commenter" role | Adopt 17.2's four-role set as canonical and reconcile 11.5/12.3 to it, removing the Viewer/Commenter overlap:  1. Define one canonical project-role enum in 17.2 with an explicit capability matrix: Admin (edit + invite/remove collaborator… |
| E-16 | 12.1 / 16.4 | ambiguity | Soft-lock release semantics differ: "mouse-up or 10-second timeout" vs "10-second heartbeat timeout" | Separate the two timers, define the (currently undefined) heartbeat, and align 12.1, 16.4, and the glossary. Minimal edits:  1) Section 12.1, replace "Lock released on mouse-up or 10-second timeout." with: "Lock is held for the entire ac… |
| E-17 | 12.3 | gap | Approval workflow does not specify lock/behaviour if the project is edited after approval | In Section 12.3, add one state-machine rule after the "On all reviewers approved" bullet (line 1141): "Approval is content-bound. Any edit that mutates the project JSON (clip, track, overlay, caption, effect, marker, or canvas/settings c… |
| F-3 | 14.5 | error | project:patch payload typed as 'Immer patch array' but glossary defines Immer Patch as RFC 6902 | Fix the Glossary entry only (the operational sections 11.3 / 12.1 / 14.5 are internally consistent with each other and with the Immer-based stack, so they should remain the source of truth). In Section 18, line 1577-1578, replace the def… |
| F-7 | 15.2 | inconsistency | Free tier can create a 4K (4096x4096) canvas but cannot export above 1080p, with no stated handling | Resolve the cap-vs-canvas tension at all three touchpoints, choosing one explicit behaviour. Recommended (matches the existing 16.1 plan-limit UX pattern):  1. Section 10.1 (Resolution row): Add — "Resolution options above the user's pla… |
| F-8 | 15.2 | inconsistency | Enterprise 'Unlimited' tracks contradicts hard per-project track caps in Section 3.2 | Resolve the 3.2-vs-15.2 contradiction by making the Enterprise track cells concrete and consistent with the engine. Minimal option (recommended): in Section 15.2 replace the three "Unlimited" track values with the actual engine ceilings … |
| F-13 | 16.3 | gap | H.265->H.264 silent fallback breaks HDR exports with no defined handling | In Section 16.3, "Codec not licensed" row, qualify the fallback so it is conditioned on color space rather than applying H.264 unconditionally: "If the requested output is SDR (Rec.709/P3-D65 SDR), fall back to H.264 automatically and no… |
| F-14 | 16.4 | inconsistency | Conflict resolution granularity contradicts itself (clip-level vs per-field) and mixes OT with last-write-wins | Edit Section 11.2 line 1058 to align with the model already described in 12.1 and 16.4. Replace:  "Conflict resolution: if two collaborators edit simultaneously, last-write-wins per field using Operational Transform on clip-level propert… |
| F-11 | 17.2 | inconsistency | Project role set (adds 'Commenter') contradicts the collaborator roles in Section 11.5 | Make 17.2 the single source of truth's downstream consumer rather than a competing definition. In Section 17.2 line 1528, change "project-level roles (Admin, Editor, Viewer, Commenter)" to "project-level roles (Admin, Editor, Viewer)" so… |
| F-12 | 17.4 | inconsistency | Export rate limit (5/min) is incompatible with Business's 10 concurrent exports | In Section 17.4, make the export-endpoint rate limit plan-aware so a user can launch their full concurrent-export allowance in one burst. Replace "export endpoint (5 req/min)" with a per-plan limit set to at least each tier's Concurrent-… |
| F-16 | 18 | error | Glossary: Immer Patch defined as RFC 6902 is technically incorrect | In Section 18 Glossary, change the "Immer Patch" definition from: "A JSON patch (RFC 6902 format) describing the difference between two states. Used for undo/redo and collaborative sync." to: "A patch object produced by Immer (shape { op… |
| A-11 | 2.1 | gap | Canvas-area height formula omits status bar and transport bar height budgets | In the Section 2.1 layout table, (a) add a "Transport Bar" row with a fixed height (e.g., "Fixed 48px height — playback controls; centred below canvas, above timeline" — reconciling it with Section 5.3's placement), and (b) correct the C… |
| A-12 | 2.2 | ambiguity | Edge handle drag behaviour is self-contradictory (non-proportional vs 'same as corner') | Edit only the "Edge handle drag" Notes cell (line 157); leave the corner row (lines 152-153) unchanged since it already defines corner = proportional and Shift-on-corner = unconstrained/free aspect. Replace the contradictory note with a … |
| B-2 | 3.2 | error | Canvas 2D globalCompositeOperation has no 'Add' or 'Subtract' modes | Revise the Blend Mode "Technical Spec / Notes" cell (line 215) to specify per-mode preview implementation instead of the blanket "globalCompositeOperation" claim. Suggested replacement: "Preview: Normal→source-over; Multiply/Screen/Overl… |
| D-10 | 4.1 | gap | HEIC 'auto-converted' and GIF 'treated as animated clip' lack conversion specifics | In Section 4.2, extend the on-upload async job list to cover the two non-standard image formats, anchoring to the existing pipeline conventions:  1. HEIC: add a server-side decode job that converts the original HEIC to a stored JPG (or P… |
| D-3 | 4.2 | inconsistency | Single 720p proxy contradicts 4K-proxy playback performance target | Make Section 4.2 the single source of truth for the proxy rendition set, and have all consumers reference only renditions it actually produces.  1. Section 4.2, task (a): replace the single fixed "720p/2Mbps" proxy with the rendition set… |
| A-7 | 5.1 / 5.2 | error | rAF cannot run 'at the project's target frame rate'; and OffscreenCanvas claim is loose | Two minimal text edits in Section 5.1.  1) Line 455 — replace "...composites them on the HTML5 Canvas using requestAnimationFrame at the project's target frame rate, and plays audio via Web Audio API nodes — all synchronised to a master … |
| A-6 | 5.1 vs 1.3 vs 6.1/6.4/6.6 | inconsistency | Compositing surface is inconsistently described as Canvas 2D vs WebGL shader | Add one architectural sentence reconciling the preview pipeline, anchored to the primitives the spec already names (Section 1.3 "OffscreenCanvas + transferToImageBitmap" in 5.1). Recommended minimal edit in Section 5.1 (and a cross-refer… |
| A-8 | 5.1 vs 13.1 / 3.4 | inconsistency | Playback-rate set differs between Playback Engine and shortcut/timeline sections | Fix the single offending sentence in Section 5.1 (line 466), treating 3.4/13.1 as authoritative (they already agree). Replace:  "Normal (1×), 0.25×, 0.5×, 2×, 4× via controls or J/K/L keys."  with:  "Fractional/slow rates (0.25×, 0.5×) a… |
| B-9 | 6.5 | gap | Behavior with exactly one remaining keyframe is unspecified | Replace the single line at 6.5 / line 629 ("If only 2 keyframes remain and both deleted: property reverts to static value.") with an explicit lifecycle that covers the one- and zero-keyframe states and aligns with the existing stopwatch/… |
| C-9 | 7.1 | gap | Audio Ducking provides no voice-detection or track-selection mechanism | Replace the Audio Ducking row's Technical Spec notes (Section 7.1, line 683) with a definition that matches the section's dual preview/export convention. Specify: (1) Trigger track = the dedicated Voice-Over track by default, with a drop… |
| C-3 | 7.2 | error | DynamicsCompressorNode has no makeup-gain parameter | Edit Section 7.2 Compressor row (line 693) Technical Spec cell. Change "DynamicsCompressorNode. FFmpeg: acompressor." to: "DynamicsCompressorNode for threshold/knee/ratio/attack/release; makeup gain implemented as a trailing GainNode imm… |
| C-4 | 7.2 | error | FFmpeg has no "convolver" filter for reverb | In Section 7.2, Reverb row, replace the Technical Spec text:  "ConvolverNode with impulse response files. FFmpeg: aecho or convolver filter."  with:  "Preview: per-clip dry/wet topology — input fans out to a dry GainNode and to a Convolv… |
| C-15 | 8.1 / 10.3 | gap | Text shadow blur radius is not reproducible by FFmpeg drawtext on burned-in export | In Section 8.1 (Text Shadow, line 767) and Section 10.3 (line 1006), resolve the unreproducible mapping for text-shadow blur. drawtext only supports shadowcolor/shadowx/shadowy (no blur), so the current "FFmpeg drawtext shadow params" ma… |
| C-10 | 9.2 / 9.1 | inconsistency | Translate "Preserves timing exactly" conflicts with the 42-char/2-line caption constraint | In Section 9.2, replace the Translate row's "Preserves timing exactly" with an explicit definition and overflow policy:  "Preserves block start/end times exactly (1:1 mapping of source blocks to translated blocks; no blocks are added, re… |
| C-11 | 9.3 / 9.2 / 9.1 | gap | Karaoke word-highlight depends on word timestamps not guaranteed for Fast model or translated/imported tracks | In Section 11.1, extend the caption block data model (line 1047) to add an optional words[] field: each entry {text, startMs, endMs}, populated only by Whisper auto-caption. In Section 9.3 "Word highlight (Karaoke mode)", add: "Karaoke r… |
| E-4 | Glossary (18) / 11.3 / 12.1 | error | Immer patches are not RFC 6902 JSON Patches | In Section 18 Glossary (line 1578), replace the definition of "Immer Patch" with: "A patch object produced by Immer describing the difference between two states (op: 'add' \| 'remove' \| 'replace'; path as an ARRAY of keys/indices; value… |
| X-7 | Section 1.3 / Section 3.2 vs Section 7.1 | inconsistency | Audio pan node type conflict: PannerNode vs StereoPannerNode | Standardise on StereoPannerNode (matches the already-correct Section 7.1, line 671, and is the valid node for the -100..+100 L/R pan). Two edits, both renaming PannerNode -> StereoPannerNode: 1. Line 58 (Section 1.3): change "per-track G… |
| X-12 | Section 10.1 (Watermark) vs Section 15.2 (Remove watermark) | gap | Forced Free-tier watermark not reflected in the Export modal description | Make the forced Free-tier watermark explicit in the export pipeline and distinguish it from the user's custom watermark. Minimal edits:  - Section 10.1, Watermark row (lines 965-967): add a clarifying clause, e.g. "This is the user's own… |
| X-6 | Section 3.2 (Audio Track) vs Section 5.3 / Section 7 (general mixing expectation) | ambiguity | Mute/Solo ignored on export is a preview-vs-export surprise | Adopt the least-surprising behavior and make it consistent end to end. Specifically:  1. Section 3.2, line 240 — replace the current sentence with: "Mute/solo state persists in project JSON AND is respected on export: muted tracks are ex… |
| X-9 | Section 3.2 vs Section 9.1 / Section 9.2 | inconsistency | Caption block text limit stated as '30 chars' display vs '42 chars/line, 2 lines' content limit, plus button-name mismatch | Standardise the caption-generation button label to one canonical string across the spec. Pick "Auto-Caption" and update line 258 (Section 3.2) from 'Button on track header: "Auto-generate".' to 'Button on track header: "Auto-Caption".' s… |
| X-11 | Section 4.2 vs Section 14.2 | inconsistency | Upload pipeline endpoints don't match the API reference (check-duplicate vs presign/confirm) | Reconcile the upload-pipeline endpoint contract between Section 4.2 and Section 14.2. Pick one of two minimal options:  (A) Fold dedup into presign (preferred, fewest moving parts): In Section 4.2 line 414, replace "POST /api/v1/assets/c… |

## Low-severity (21)

| ID | Section | Type | Issue | Resolution (summary) |
|----|---------|------|-------|----------------------|
| A-17 | 1.3 | ambiguity | 'decode runs in SharedArrayBuffer worker' conflates a memory primitive with a worker, and SAB is not required for WebCodecs | Reword line 56 (Section 1.3, Media Decode row) to: "WebCodecs API VideoDecoder (Chrome/Edge) with ffmpeg.wasm fallback for Safari/Firefox; decode runs in a dedicated Web Worker. Only the ffmpeg.wasm fallback uses SharedArrayBuffer (for m… |
| D-14 | 10.1 | ambiguity | Audio Bitrate 'Lossless' and Audio Codec are underspecified for non-WAV containers | In Section 10.1, scope "Lossless" to the PCM/WAV path the spec already supports rather than adding new codecs. Change the Audio Bitrate row note to: "64 / 128 / 192 / 256 / 320 Kbps / Lossless. 'Lossless' is selectable only when Output F… |
| E-14 | 11.3 / 13.2 | inconsistency | Redo shortcut set differs: Ctrl+Shift+Z listed in 11.3 but absent from the 13.2 reference | In Section 13.2, change the single combined row "Ctrl + Z / Y \| Undo / Redo" to include the Ctrl+Shift+Z redo binding already documented in 11.3. Minimal edit, preserving the table's compact style — split into two rows:    Ctrl + Z     … |
| E-15 | 13 / 12.2 | gap | Comment tool C key is missing from the "Full" keyboard shortcut reference | Add one row to the existing Section 13.3 (View & Navigation) table — do not create a new subsection. Insert after the backtick row:  "C \| Activate comment tool, then click canvas to add a comment (active only when not in inline text/cap… |
| F-9 | 15.2 | gap | Caption-track limit missing from plan table | Add a single "Max caption tracks" row to the Section 15.2 Plan Limits table, placed adjacent to the other track-limit rows (after "Max overlay tracks"). Tighten the values to follow the table's established, invariant conventions rather t… |
| F-15 | 16.1 | error | 'File too large' toast hard-codes a GB unit, wrong for image (100 MB) limit | In Section 16.1, split the single "File too large" row into two rows with cause-specific messages, and drop the hard-coded GB unit in favor of a unit-aware placeholder (formats sizes as MB below 1 GB, GB at or above):  - Per-file (format… |
| F-17 | 18 | ambiguity | Glossary CRF range scoped only to H.264 while export offers H.265/VP9 with different ranges | In Section 10.1, Video Bitrate row, replace the single "CRF default: 18 (visually lossless)" with per-codec Auto-mode quality targets, and update the Glossary CRF entry to note that the scale is codec-dependent:  Section 10.1 (Video Bitr… |
| A-15 | 2.1 | ambiguity | Right panel 'hidden when nothing selected' conflicts with always-available panels it must host | Amend Section 2.1 Right Panel ("Technical Spec / Notes") to scope the auto-hide rule to the inspector role and define precedence:  Replace: "Context-sensitive properties inspector. Shows clip properties, text style, audio envelope, or ex… |
| A-13 | 2.1 vs 2.2 | inconsistency | Two different 'default background' colours for the canvas area (#1A1A2E vs #111111) | In Section 2.1, change the Canvas Area note to name the non-exported editor surround explicitly, e.g.: "Preview viewport centred inside; the surrounding editor area (letterbox/pillarbox surround) is neutral dark (#1A1A2E) and is editor c… |
| A-14 | 2.1 vs 6.4 | inconsistency | Left-panel tab enumeration omits the Transitions tab referenced elsewhere | In Section 2.1, Left Panel row (line 103), add "Transitions" to the tab enumeration so it reads: "Media library tabs: Videos, Audio, Images, Text overlays, Stickers, Captions, Transitions." (Optionally, since Transitions are effect objec… |
| B-10 | 3.6 | error | AnalyserNode FFT is not a valid mechanism for offline beat/transient detection | In Section 3.6, line 395, replace the client-side mechanism only. Change:  "Uses client-side Web Audio AnalyserNode FFT or server-side Essentia.js WASM. Accuracy ±1 frame at 30fps."  to:  "Uses client-side offline onset detection (decode… |
| D-16 | 4.3 | gap | Stock library import licensing/attribution lacks a data model and the formats span video/image/audio ambiguously | Make the stock-library promise on line 450 implementable with three minimal additions:  1. Asset model (Section 14.2 / wherever the asset schema is canonicalized): add an optional `source` object to the asset record for stock-imported as… |
| A-16 | 5.1 vs 5.2 | inconsistency | Blanket seek-latency target (<200ms) contradicts the per-scenario targets in 5.2 | In Section 5.1, replace the sentence "Seek latency target: <200ms for proxy files." with a deferral to the per-scenario table rather than a competing number, e.g.: "Seek latency targets vary by project complexity — see the per-scenario f… |
| A-10 | 5.2 | error | Audio latency target of <10ms via AudioContext is unrealistic as a hard guarantee | In Section 5.2, replace the "Audio latency (live edit): <10ms via AudioContext — imperceptible" row value with: "AudioContext output latency typically <30ms with latencyHint:'interactive'; platform-dependent, measured at runtime via Audi… |
| C-17 | 8.1 / 10.3 | gap | Text gradient fill and outline position (inside/centre) not reproducible via drawtext export | Add an export-fidelity note to the affected Section 8.1 rows and a matching clause to Section 10.3, so the export path is unambiguous:  1. Section 8.1, Colour row (Technical Spec column, after "Gradient applied via Canvas fillStyle gradi… |
| C-12 | 9.1 | ambiguity | Caption source ambiguity: modal selects a "source audio track" but backend extracts from a single video clip | Tightened to match the existing API shape (single assetId source + destination caption trackId), rather than the auditor's track-render-to-WAV design which the API does not model.  Edit 9.1 to make all three artifacts consistent: - Step … |
| C-13 | 9.1 | gap | "Speaker change detection creates new block" lacks any diarization/labeling specification | Minimal fix: delete the sentence "Speaker change detection creates new block." from Section 9.1 (line 824), since Whisper/Faster-Whisper (the only defined transcription engine) cannot produce speaker boundaries and no other part of the s… |
| C-14 | 9.3 / 3.2 | gap | Translate can exceed the 4-caption-track limit with no defined behavior | In Section 16 (Error Handling), add a "Caption track limit reached" case covering any action that would create a 5th caption track (Translate Track, auto-caption, or caption import): block the action and show a toast such as "Caption tra… |
| X-15 | Section 10.1 (Audio Channels '5.1 Surround') vs Section 7 (Audio Engine) | gap | 5.1 Surround export option unsupported by the documented stereo-only audio engine | Minimal, precise fix: remove the 5.1 Surround option from Section 10.1, line 919, so the Audio Channels row reads: "Stereo (default) / Mono." This aligns the export modal with the stereo-only audio engine (Section 7) and playback (Sectio… |
| X-13 | Section 18 (Glossary) vs Section 1.3 / Section 12.1 | error | Immer patch described as RFC 6902 JSON Patch, which it is not | Replace the line 1578 definition with: "A patch object produced by Immer (op of add/replace/remove, with path as an ARRAY of keys, plus value). NOT RFC 6902/JSON Pointer format — apply with Immer's applyPatches, do not use generic json-p… |
| X-14 | Section 3.2 (Track limit) vs Section 15.2 (Plan limits) | ambiguity | Track limits stated as absolute caps in Section 3 contradict Enterprise 'Unlimited' | Make Section 3.2 defer to the plan table instead of stating absolutes. Change each "Track limit" cell to reference the plan-dependent limits: - Line 202: "Up to 20 video tracks per project on Pro/Business; Free is limited to 3, Enterpris… |
