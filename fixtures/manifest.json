{
  "schemaVersion": "1.0",
  "notes": [
    "Synthetic CC0 fixtures generated by scripts/generate-fixtures.ts (lavfi sources).",
    "Golden frames live under fixtures/golden/ as REGULAR PNG GIT BLOBS (no Git LFS) —",
    "they are small (640x360) and diff-reviewable. Goldens are encoder-build-specific:",
    "regenerate ONLY via `pnpm generate:fixtures --goldens` inside the pinned FFmpeg 6.1.1",
    "env, as a reviewed commit citing the trigger (FFmpeg bump / intentional graph change)."
  ],
  "fixtures": [
    {
      "id": "fixture-gold-bars-5s",
      "filename": "gold_bars_5s.mp4",
      "contentType": "video/mp4",
      "license": "CC0",
      "durationMs": 5000,
      "width": 640,
      "height": 360,
      "synthetic": "lavfi testsrc2 (per-frame counter — trim/speed offsets visible)",
      "usedBy": ["golden:trim", "golden:speed"],
      "md5": "generated-at-build-time",
      "s3Key": "fixtures/gold_bars_5s.mp4",
      "description": "5s 640x360 testsrc2 counter clip — drives the trim + speed golden fixtures."
    },
    {
      "id": "fixture-gold-red-3s",
      "filename": "gold_red_3s.mp4",
      "contentType": "video/mp4",
      "license": "CC0",
      "durationMs": 3000,
      "width": 640,
      "height": 360,
      "synthetic": "lavfi color=red",
      "usedBy": ["golden:stack"],
      "md5": "generated-at-build-time",
      "s3Key": "fixtures/gold_red_3s.mp4",
      "description": "3s solid-red clip — bottom (z-order 0) layer of the stack/overlay golden."
    },
    {
      "id": "fixture-gold-blue-3s",
      "filename": "gold_blue_3s.mp4",
      "contentType": "video/mp4",
      "license": "CC0",
      "durationMs": 3000,
      "width": 640,
      "height": 360,
      "synthetic": "lavfi color=blue",
      "usedBy": ["golden:stack"],
      "md5": "generated-at-build-time",
      "s3Key": "fixtures/gold_blue_3s.mp4",
      "description": "3s solid-blue clip — top (z-order 1) PiP layer of the stack/overlay golden."
    },
    {
      "id": "fixture-bunny-3s",
      "filename": "bunny_h264_3s.mp4",
      "contentType": "video/mp4",
      "license": "CC0",
      "durationMs": 3000,
      "width": 1280,
      "height": 720,
      "md5": "placeholder-will-be-computed",
      "s3Key": "fixtures/bunny_h264_3s.mp4",
      "description": "3-second 720p H.264 test clip (smptehdbars synthetic, CC0)"
    },
    {
      "id": "fixture-audio-5s",
      "filename": "tone_440hz_5s.wav",
      "contentType": "audio/wav",
      "license": "CC0",
      "durationMs": 5000,
      "md5": "placeholder-will-be-computed",
      "s3Key": "fixtures/tone_440hz_5s.wav",
      "description": "5-second 440Hz sine wave (generated, CC0)"
    }
  ],
  "golden": {
    "storage": "regular PNG git blobs (NOT Git LFS) under fixtures/golden/",
    "encoder": "FFmpeg 6.1.1 (pinned — see Pipeline.md §2.6 / scripts/check-ffmpeg.mjs)",
    "thresholds": { "ssimMin": 0.985, "psnrMinDb": 38, "class": "lossy H.264 @ Auto-CRF (§22.3)" },
    "regenerate": "pnpm generate:fixtures --goldens   (reviewed; pinned FFmpeg only)",
    "frames": [
      "trim_t1000.png",
      "stack_t1000.png",
      "speed_t1000.png"
    ]
  }
}
